# Angular 22 Showcase Application Prompt

You are working as an expert Angular architect, senior front-end engineer, and user experience specialist.

I need you to help me build an Angular 22 web application that is modern, professional, responsive, visually beautiful, attractive, and well organized.

The goal of this application is to serve as a showcase, playground, and testing dashboard for Angular 22 features, components, patterns, and new framework capabilities.

The project has already been created with `ng new`. It only contains the base structure generated by Angular CLI. You do not need to initialize the project.

Before writing or modifying any file, you must first analyze the existing project and create a detailed implementation plan.

Do not implement anything yet.

First, propose the best options, application structure, sections, visual approach, architecture, routing strategy, component organization, i18n strategy, public API strategy, testing strategy, and development phases.

Wait for my confirmation before starting the implementation.

---

## Official Reference

Use the official Angular documentation as the main source of truth:

https://angular.dev/

Before proposing or implementing anything, review and validate Angular 22 features against the official Angular documentation.

Do not assume APIs, syntax, or features if they are not documented, stable, or available in Angular 22.

If any requested feature is experimental, in developer preview, renamed, removed, or not officially supported, explain it clearly and propose the safest alternative.

---

## Project Stack

The project will use this stack:

```json
{
  "name": "angular-22-app",
  "version": "0.0.0",
  "engines": {
    "node": ">= 24.16.0",
    "pnpm": ">= 11.5.3"
  },
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build",
    "watch": "ng build --watch --configuration development",
    "test": "ng test"
  },
  "private": true,
  "packageManager": "pnpm@11.5.3",
  "dependencies": {
    "@angular/common": "^22.0.0",
    "@angular/compiler": "^22.0.0",
    "@angular/core": "^22.0.0",
    "@angular/forms": "^22.0.0",
    "@angular/platform-browser": "^22.0.0",
    "@angular/router": "^22.0.0",
    "rxjs": "~7.8.0",
    "tslib": "^2.3.0"
  },
  "devDependencies": {
    "@angular/build": "^22.0.0",
    "@angular/cli": "^22.0.0",
    "@angular/compiler-cli": "^22.0.0",
    "@tailwindcss/postcss": "^4.1.12",
    "jsdom": "^28.0.0",
    "postcss": "^8.5.3",
    "prettier": "^3.8.1",
    "tailwindcss": "^4.1.12",
    "typescript": "~6.0.2",
    "vitest": "^4.0.8"
  }
}
```

Use Angular 22, TypeScript 6, Tailwind CSS 4, Vitest, and pnpm.

Do not change the package manager. Use `pnpm`.

Do not run `ng new`.

---

## Project Context

The project already exists. It only has the base structure generated by `ng new`:

- `src/app/app.component.ts` with an empty root component
- `src/app/app.config.ts` with basic configuration
- `src/app/app.routes.ts` empty
- `angular.json`, `tsconfig.json`, `package.json` already configured
- No routes, no additional components, no custom styles

Your work is to build on top of this base.

Before planning, review:

- `package.json`
- `angular.json`
- `tsconfig.json`
- `src/app/app.config.ts`
- `src/app/app.routes.ts`
- `src/app/app.component.ts`
- the real project structure

Do not assume that a folder, file, dependency, or configuration exists if you have not verified it.

---

## Angular 22 Context to Validate

The following features are main showcase candidates. You must validate them against the official documentation before implementing them:

### Defaults
- OnPush as the default change detection strategy.
- `ChangeDetectionStrategy.Eager` as the alternative to the previous behavior.
- HTTP Client using Fetch by default.
- Incremental Hydration when using `provideClientHydration()`.

### Signal Forms
- `@angular/forms/signals` API.
- `form()`.
- `FieldTree`.
- Official field binding mechanism for templates.
- Signal-based form state.
- `getError()`.
- `reloadValidation()`.
- New validators such as `minDate()` and `maxDate()`, if available.
- `debounce`.
- `validateAsync()`.
- `validateHttp()`.
- `FormValueControl`.
- Compatibility or migration from `ControlValueAccessor`.

### Resource API
- `resource()`.
- `rxResource()`.
- `httpResource()`.
- States: `value`, `error`, `isLoading`, `status`.
- Automatic cancellation or race-condition handling.
- `reload()`.

### Reactivity and DI
- `linkedSignal()`.
- `debounced()`, if it exists in Angular 22, and mark it as experimental if the documentation says so.
- `@Service()`.
- `injectAsync()`.
- `inject()`.

### Angular ARIA
- `@angular/aria`, if available and stable.
- If it requires installing an additional dependency, propose the dependency and justify why it is necessary before modifying the project.

### Router
- Reactive `isActive`, if available.
- Automatic route injector cleanup.
- Native navigation improvements, if documented.

### Templates
- Comments inside attribute declarations, if documented.
- `@switch` improvements.
- `@if`, `@for`, `@switch`, `@defer`, `@let`.
- Spread syntax and arrow functions in templates only if officially supported.

Important:

Do not blindly implement anything from this list. If something is not documented, do not use it as a stable feature.

---

## Main Objective

Create an Angular 22 showcase application where the user can open the page and navigate through different sections to see, test, and understand Angular 22 features.

The application should feel like:

- A modern technical dashboard
- A documentation playground
- A visual demo center
- A component lab
- A practical Angular 22 reference

It must not look like a basic generated Angular application.

The UI must be:

- Modern
- Professional
- Beautiful
- Visually attractive
- Responsive
- Clean
- Organized
- Easy to navigate
- Useful for learning and testing Angular 22

---

## Visual Design Requirements

Create a polished visual experience.

The application must include:

- A modern home page or dashboard with a hero section
- Collapsible left sidebar + top navbar
- Clear feature cards with representative icons
- Section navigation with active-route indicator
- Mobile drawer with hamburger menu
- Good spacing and typography
- Dark or neutral backgrounds with Angular brand accents (`#dd0031` / `#c3002f`)
- Shadows, borders, glass effects, or cards where useful
- Clear visual hierarchy
- Professional palette with dark mode as default
- Design that feels like a real product
- Status badges per section: `STABLE`, `EXPERIMENTAL`, `NEW IN V22`
- Lightweight charts in the dashboard or sections where they add value

Propose visual direction options before implementation if useful.

The plan confirmation is enough to proceed.

---

## JSON i18n Requirement

Implement runtime internationalization using JSON files.

Required languages:

- English
- Spanish

Rules:

- The default language must be English.
- The user must be able to switch between English and Spanish from the UI.
- The language selector must be visible in the top navbar or a clear settings area.
- If the user selects another language, the browser must remember it when the user comes back.
- Use local persistence only for the language preference, for example `localStorage`.
- Do not store sensitive data in `localStorage`.
- Provide an English fallback if a translation key is missing.
- Update the document `lang` attribute according to the active language.
- Navigation labels, titles, buttons, messages, badges, forms, tables, states, and main text must be translated.
- Code examples can remain in English.
- Code identifiers must always be in English.

Suggested structure:

```txt
src/
  assets/
    i18n/
      en.json
      es.json
  app/
    core/
      i18n/
        language.model.ts
        translation.service.ts
        translation.loader.ts
        translation.pipe.ts
```

You may adjust this structure if you find a better option.

Suggested implementation:

- Create a signal-based `TranslationService`.
- Load JSON files from `assets/i18n`.
- Keep `currentLanguage` as a signal.
- Expose a function or pipe to translate keys.
- Persist the selected language.
- Load English by default.
- Apply fallback per key.
- Avoid adding external libraries such as `ngx-translate` unless you clearly justify why they are necessary.

Include tests for:

- Default language.
- Language switching.
- Language persistence.
- English fallback.
- Basic key translation.

---

## Public API and Data Requirement

If you need to build tables, lists, data cards, charts, or HTTP consumption demos, you can use PokeAPI:

https://pokeapi.co/docs/v2

Prefer PokeAPI for:

- Tables
- Paginated lists
- Record details
- `httpResource()` demos
- Loading/error/reload demos
- Reactive parameter demos
- Master-detail demos

Important PokeAPI notes:

- Use paginated endpoints with `limit` and `offset` when applicable.
- Do not implement local pagination if the goal is to demonstrate service-side pagination.
- For search, first validate whether the endpoint supports partial search.
- If PokeAPI does not support partial search for the required case, implement exact search by name/id or propose another public API that supports service-side search and pagination.
- Do not simulate local search as if it were backend search.
- Handle loading, error, empty, and retry states.
- Strongly type response models.

If you need other resources, consider this repository to select public APIs:

https://github.com/public-apis/public-apis

Rules for external APIs:

- Prefer APIs that do not require a token.
- Prefer APIs that do not require registration.
- Avoid APIs with CORS problems.
- Avoid unstable APIs or APIs without clear documentation.
- Do not include tokens, API keys, or credentials.
- Centralize endpoints in services or constants.
- Do not consume APIs directly from components.
- Document in the plan which API will be used and why.

Suggested structure:

```txt
src/
  app/
    core/
      api/
        poke-api.client.ts
        public-api.model.ts
    features/
      data-explorer/
        pokemon-table.page.ts
        pokemon-detail.page.ts
        pokemon-table.service.ts
        models/
```

---

## Remote Table with Service-Side Pagination, Search, and Detail

Implement a real demo that displays a table with remote data.

It can be a highlighted widget in the main dashboard or its own section called `Data Explorer`.

It must include:

- Professional-looking table.
- Data consumed through an HTTP service.
- Service-side pagination, not local pagination.
- Configurable page size.
- Search input.
- Search debounce if applicable.
- Loading state.
- Empty state.
- Error state with retry button.
- Row click to view details.
- Detail view in a side panel, modal, drawer, or detail route.
- Breadcrumb or clear navigation.
- URL state with query params for page, page size, and search if reasonable.
- Integration with `httpResource()` if applicable.
- Strong typing for responses.
- Organized services and models.
- i18n translations for headers, buttons, messages, and states.

Recommended implementation with PokeAPI:

- Pokémon table using `/pokemon?limit={pageSize}&offset={offset}`.
- Details using `/pokemon/{name}` or `/pokemon/{id}`.
- Columns such as:
  - ID, if available from the detail request or derived from the URL
  - Name
  - URL
  - Actions
- When selecting a row, load real details:
  - Image
  - Types
  - Height
  - Weight
  - Base experience
  - Abilities
  - Main stats

Important:

PokeAPI supports pagination through `limit` and `offset`, but you must not assume that it supports partial search in all endpoints.

If a partial-search experience with real backend pagination is required, you may propose a second no-token public API that supports both server-side search and pagination.

---

## Charts Requirement

You may implement charts freely in tests or in the dashboard, without exaggerating.

Charts must add visual and technical value.

Useful examples:

- Pokémon type distribution chart.
- Resource state chart: resolved, loading, error.
- Feature usage by category.
- Render comparison chart in the Performance section.
- Mini charts in dashboard cards.
- Stat bars in Pokémon detail.
- Simple chart to demonstrate signals and derived state.

Rules:

- Prefer lightweight implementations with SVG, CSS, and Angular before adding an external library.
- If you propose a library such as Chart.js, ApexCharts, or ECharts, justify the dependency and wait for confirmation.
- Do not overuse charts.
- Do not invent data that looks real without clarifying it is demo data.
- Keep charts accessible: labels, alternative text, or descriptions.
- Translate chart titles and legends with i18n.

---

## Application Structure

The application must be organized into sections where each Angular 22 feature or group of features can be tested independently.

Use a clean Angular architecture with:

- Standalone components
- Feature-based organization
- Lazy-loaded routes for each feature
- Reusable UI components in `shared/ui`
- Clear separation between layout, features, shared UI, models, and utilities
- Strong typing with TypeScript 6
- No unjustified `any`
- Signals where appropriate
- OnPush as default without specifying it in each component unless necessary
- Clean, readable, and well-organized code

Suggested structure:

```txt
src/
  assets/
    i18n/
      en.json
      es.json
  app/
    core/
      api/
      i18n/
      services/
      guards/
    layout/
      shell/
      sidebar/
      navbar/
    shared/
      ui/
        badge/
        button/
        card/
        chart/
        code-preview/
        data-table/
        tabs/
        accordion/
        modal/
        toast/
        empty-state/
        loading-state/
      models/
      utils/
    features/
      dashboard/
      data-explorer/
      signals/
      signal-forms/
      reactive-forms/
      templates/
      router/
      resources/
      aria-accessibility/
      di-patterns/
      performance/
      components-lab/
    app.config.ts
    app.routes.ts
```

Adjust this structure if you find a better option.

---

## Required Sections

The application must include a main dashboard and navigation to the following sections. Each section must have a visible badge indicating whether the feature is `STABLE`, `NEW IN V22`, or `EXPERIMENTAL`.

---

### 1. Dashboard / Home

Create a visually attractive home page that explains the purpose of the application.

It must include:

- Hero section with Angular logo and the title "Angular 22 Showcase".
- Short explanation of the showcase.
- Grid of cards for the main feature groups.
- Status badges.
- Direct links to sections.
- Visual summary of important Angular 22 features.
- Highlighted remote table widget or data summary if you consider it better here.
- Lightweight chart or mini charts if they add value.
- Responsive design.
- i18n translated text.

---

### 2. Data Explorer / Remote Table

Create a dedicated section for remote data consumption.

It must demonstrate:

- `httpResource()` or reactive HTTP service.
- Table with remote data.
- Service-side pagination.
- Search.
- Debounce.
- Query params.
- Row click for detail.
- Detail view.
- Loading, error, empty, retry.
- Translations.
- Strong typing.

API preference:

- PokeAPI for pagination and details.
- Another no-token public API if real partial backend search is required.

This section must be visually useful and feel like a real product feature.

---

### 3. Signals Section

Create a section to demonstrate modern Angular Signals.

Include interactive examples for:

- Basic `signal()` with `set()` and `update()`.
- Do not use `mutate()` unless you confirm it officially exists in Angular 22.
- `computed()` with multiple dependencies.
- `effect()` with cleanup.
- `linkedSignal()` if available.
- `debounced()` marked as `EXPERIMENTAL`, if available.
- Real-time derived state.
- Visual comparison between Signal and BehaviorSubject.

Each example must show:

- Live demo.
- Relevant Angular code preview.
- Short explanation.
- Translated UI text.

---

### 4. Signal Forms Section

Create a dedicated section for Signal Forms if it is stable and documented in Angular 22.

Include demos for:

- Basic form.
- Built-in validators.
- Custom validator.
- Real-time form state.
- Typed errors.
- Async validation.
- Validation reload.
- Custom control.
- Signal Forms vs Reactive Forms comparison.
- Real-time form value JSON.
- Translated text.

If any API changed from expectations, use the official syntax.

---

### 5. Reactive Forms and Template-Driven Forms Section

Create a separate section for classic Angular forms.

Include tabs for:

- Reactive Forms.
- Template-driven Forms.
- Sync and async validators.
- Dynamic forms.
- Disabled states.
- Custom validators.
- JSON preview.
- Field-level error handling.
- Translated text.

---

### 6. Resource API Section

Create a dedicated Resource API section.

Include demos for:

- Basic `httpResource()`.
- State handling: loading, error, value, status.
- Reactive parameters.
- Cancellation by returning `undefined`, if documented.
- `reload()`.
- Automatically handled race condition.
- `rxResource()`.
- `resource()`.
- `debounced()` connected to `httpResource()`.
- PokeAPI or another public no-token API.
- Translated text.

---

### 7. Template Syntax Section

Create a section to demonstrate modern Angular template syntax.

Include:

- `@if`, `@else if`, `@else`.
- `@for` with `track` and contextual variables.
- `@switch` and inference improvements.
- `@defer` with triggers.
- `@placeholder`, `@loading`, `@error`.
- `@let`.
- Property binding.
- Event binding.
- Two-way binding.
- Class/style binding.
- Attribute comments, if supported.
- Spread syntax or arrow functions only if officially supported.
- `ng-content` with slots.
- Standalone custom pipe.
- Translated text.

---

### 8. Router Section

Create a section to demonstrate Angular Router.

Include:

- Basic navigation.
- Nested routes.
- `RouterLink` with active state.
- Reactive `isActive`, if available.
- Route parameters.
- Query parameters.
- Lazy loading.
- Functional guards.
- Resolvers.
- Router events.
- Navigation state.
- Programmatic redirects.
- Route data.
- Automatic route injector cleanup, explained.
- Prefetching, if applicable.
- Internal sub-navigation.
- Translated text.

---

### 9. Dependency Injection Section

Create a section for modern DI patterns.

Include:

- `inject()` vs constructor injection.
- `@Service()`, if documented.
- Comparison with `@Injectable({ providedIn: 'root' })`.
- `injectAsync()`, if documented.
- Scoped providers.
- Route-level providers.
- `InjectionToken`.
- `@Self`, `@SkipSelf`, `@Optional`, `@Host`.
- Functional interceptors.
- Translated text.

---

### 10. Angular ARIA / Accessibility Section

Create a section focused on accessibility.

If `@angular/aria` is available and stable, propose adding the dependency if it does not exist in `package.json`.

Include:

- Accessible components or patterns.
- Buttons.
- Dialogs.
- Tabs.
- Listbox.
- Combobox.
- Form integration.
- Keyboard navigation.
- Focus trap.
- Focus restoration.
- ARIA roles.
- Labels.
- Live regions.
- Minimum WCAG AA contrast.
- Translated text.

If `@angular/aria` is unavailable or requires an installation that is not approved, implement alternatives with standard Angular and semantic HTML.

---

### 11. UI Components Lab Section

Create a visual lab where reusable components can be tested.

Include:

- Buttons.
- Cards.
- Badges.
- Tabs.
- Accordion.
- Modal/Dialog.
- Toast/Notification.
- Empty states.
- Loading states.
- Error states.
- Responsive grids.
- Code preview block.
- Form fields.
- Reusable data table.
- Simple reusable charts.
- Language selector.
- Translated text.

The lab should feel like a simplified Storybook.

---

### 12. Performance Section

Create a section that explains and demonstrates Angular 22 performance.

Include:

- OnPush by default.
- Signals and fine-grained reactivity.
- `@defer`.
- Lazy-loaded routes.
- `httpResource` and automatic request cancellation.
- `injectAsync` and code splitting, if applicable.
- Pure pipes vs template methods.
- `track` in `@for`.
- Visual best-practices checklist.
- Lightweight charts if they help explain renders or states.
- Translated text.

Do not invent benchmark numbers.

---

## Navigation Requirements

Implement:

- Left sidebar with:
  - Logo and app name.
  - Section list grouped by category.
  - Status badge per section.
  - Active-section visual indicator.
  - Collapsed mode on desktop.
  - Drawer on mobile.
- Top navbar with:
  - Hamburger menu on mobile.
  - Sidebar collapse button.
  - Breadcrumb.
  - EN/ES language selector.
  - Theme toggle if implemented.
- Quick navigation cards in the dashboard.

---

## Code Preview Requirement

All sections must include live demos and code previews when useful.

The `CodePreview` component must be reusable and show:

- Clear code.
- Copy button.
- Show/hide toggle.
- Visible language label.
- Professional design.

Avoid adding dependencies only for syntax highlighting unless you justify and propose them first.

---

## Testing Requirement

Use Vitest for tests.

Include tests for:

- Utilities in `shared/utils`.
- Signals logic.
- Signal Forms validation.
- Translation service.
- Language persistence.
- API clients.
- Pagination transformation.
- Table logic.
- Important components.
- One basic component with Angular `TestBed`.

Configure Vitest to run with `pnpm test` and be compatible with Angular 22.

Prioritize tests that run correctly over full coverage.

---

## Tailwind CSS 4 Requirement

Use Tailwind CSS 4 for styling.

- Use `@theme`, native CSS variables, and container queries if applicable.
- Create a color system aligned with Angular branding.
- Define custom variants if necessary.
- Avoid duplicated or messy classes.
- Use reusable components.
- Keep the UI consistent and accessible.

---

## Implementation Rules

When implementing:

- Keep code clean and strongly typed.
- Use English for identifiers, variables, functions, classes, interfaces, files, and components.
- Use standalone components.
- Use `inject()` instead of constructor injection where applicable.
- Use `@Service()` only if it is officially documented and available.
- Use Angular Signals for local and derived state.
- Use Resource API for reactive async data.
- Use official Angular 22 APIs.
- Use Tailwind CSS 4.
- Keep UI mobile-first.
- Keep sections modular and lazy-loaded.
- Avoid unnecessary dependencies.
- Do not add external libraries without explaining and justifying them.
- Use realistic demo data.
- Do not use Lorem Ipsum.
- Keep accessibility across the UI.
- Keep the project buildable.
- Run or suggest relevant commands after important changes.
- Do not expose tokens or credentials.
- Do not consume APIs directly from components.
- Do not use local pagination when the requirement says service-side pagination.
- Use i18n for visible text.

---

## Required First Response

Your first response must not modify files.

Your first response must include:

1. Confirmation that the base project already exists and how you will proceed on top of it.
2. Angular 22 feature validation: stable, experimental, and unconfirmed.
3. Recommended architecture.
4. JSON i18n strategy.
5. Public API strategy.
6. Strategy for the remote table with search, service-side pagination, and detail.
7. Charts strategy.
8. Recommended visual direction.
9. Proposed sections and routes with badges.
10. Proposed folder structure.
11. Implementation phases.
12. Testing strategy.
13. Required additional dependencies and justification.
14. Risks or assumptions.
15. Clear request for confirmation before implementing.

Do not start writing code until I confirm the plan.

---

## Behavior After Confirmation

After I confirm the plan, implement the application step by step.

Suggested order:

1. Base layout, shell, sidebar, navbar, Tailwind color system.
2. JSON i18n, language selector, and persistence.
3. Dashboard / home page.
4. Base API client and Data Explorer with remote table.
5. Signals section.
6. Signal Forms section.
7. Resource API section.
8. Templates section.
9. Router section.
10. DI section.
11. ARIA / Accessibility section.
12. Components Lab section.
13. Performance section.
14. Reactive / Template-driven Forms.
15. Tests.

After each phase, confirm that the project builds and navigates correctly before continuing with the next phase.

---

## Expected Final Result

The final application must allow the user to:

- Open a beautiful Angular 22 showcase page with a modern technical dashboard design.
- Switch language between English and Spanish.
- Keep the selected language remembered when returning.
- Navigate sections with an intuitive sidebar and navbar.
- Test Signal Forms, Resource API, Signals, templates, router, DI, accessibility, and UI components.
- See a real remote table with search, service-side pagination, and detail.
- Consume data from PokeAPI or another no-token public API when appropriate.
- See lightweight charts in the dashboard or specific demos.
- Understand Angular 22 features through interactive visual demos.
- See clean examples with copyable code previews.
- Clearly identify which features are new in v22, stable, and experimental.
- Use the app as a learning playground and Angular 22 reference.
